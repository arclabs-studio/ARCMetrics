# ARCDevTools Workflow Template
# Source: https://github.com/arclabs-studio/ARCDevTools/workflows/tests.yml
#
# This file was copied from ARCDevTools. You can customize it for your project.
# To update: re-run ./ARCDevTools/arcdevtools-setup or manually copy from ARCDevTools/workflows/
name: Tests

on:
  push:
    branches: [main, develop, feature/*]
  pull_request:
    branches: [main, develop]

jobs:
  test-macos:
    name: Build (macOS)
    runs-on: macos-15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Display Swift version
        run: swift --version

      - name: Run tests
        run: swift test --parallel

  test-linux:
    name: Build (Linux)
    runs-on: ubuntu-latest
    container:
      image: swift:6.0

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Display Swift version
        run: swift --version

      - name: Run tests
        run: swift test --parallel
